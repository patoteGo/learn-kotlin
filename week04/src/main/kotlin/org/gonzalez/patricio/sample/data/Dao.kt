package org.gonzalez.patricio.sample.data

// Person does not need to import, because both are in the same namespace
class PersonDao {
    fun insert(person: Person) { TODO("put in the database")}
    fun delete(person: Person) { TODO("delete from the database")}
    fun update(person: Person) { TODO("update from the database")}
    fun get(id: String) { TODO("find Person by id from database") }
}

